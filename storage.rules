rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Solo permitir que cada usuario suba y descargue sus propios archivos (canciones y karaokes)
    match /users/{userId}/{allPaths=**} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }
  }
}
